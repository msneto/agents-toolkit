{
	"name": "get_github_helper",
	"description": "Detailed description of what github-helper does for the agent.",
	"version": "1.0.0",
	"parameters": {
		"type": "object",
		"properties": {
			"query": {
				"type": "string",
				"description": "Search query or input"
			}
		},
		"required": ["query"]
	},
	"runtime": {
		"engine": "bun",
		"entrypoint": "src/index.ts",
		"timeout": 5000
	}
}
